/**
 * SelectedTagsDisplay Module (Retired)
 *
 * Kept as a no-op stub to avoid script tag errors.
 * Selected tags are now shown in the unified chip bar (FilterPanelUI).
 *
 * @module SelectedTagsDisplay
 */
const SelectedTagsDisplay = (() => {
    return {
        init() {},
        reset() {},
        render() {}
    };
})();
