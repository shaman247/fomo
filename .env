# Gemini AI API Key (required for event extraction)
GEMINI_API_KEY="(your API key, see https://ai.google.dev/gemini-api/docs/api-key)"

# FTP Configuration for uploading exported JSON files
FTP_HOST="your-ftp-server.com"
FTP_USER="your-username"
FTP_PASSWORD="your-password"
FTP_REMOTE_DIR="data"  # Optional: remote directory path (leave empty for root)
