<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- System bar colors are handled by enableEdgeToEdge() in MainActivity;
         android:statusBarColor / android:navigationBarColor are deprecated
         as of Android 15 and must not be set here. -->
    <style name="Theme.Fomo" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>

</resources>
