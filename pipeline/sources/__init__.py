# Source plugin package. Each <platform>.py module here exports a module-level
# `PROFILE` (a site_profiles.SiteProfile) or `PROFILES` (an iterable of them),
# auto-discovered by site_profiles._discover_profiles(). Real plugins are
# gitignored; copy a *.example.py to start. See README.md.
